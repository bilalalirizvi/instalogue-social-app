import React from "react";
import { CircularProgress } from "@mui/material";
const Progress = () => {
  return (
    <CircularProgress color="inherit" size="25px" sx={{ marginTop: "4px" }} />
  );
};

export default Progress;
